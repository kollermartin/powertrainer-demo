import { NotificationConfig } from "antd/lib/notification/interface";

const defaultNotificationConfig: Partial<NotificationConfig> = {
    placement: "top",
};

export default defaultNotificationConfig;
